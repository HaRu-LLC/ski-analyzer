services:
  backend:
    build:
      context: .
      dockerfile: infrastructure/Dockerfile.gpu
    environment:
      - USE_MOCK=false
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
